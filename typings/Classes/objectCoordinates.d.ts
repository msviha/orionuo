declare interface ObjectCoordinates {
    /**
     * int pos.X();
     * Returns: x coordinate.
     */
    X(): number;

    /**
     * int pos.Y();
     * Returns: y coordinate.
     */
    Y(): number;
}
